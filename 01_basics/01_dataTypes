"use strict" ;  //treat all JS code as newer version

// alert (3 + 3) //we are using node js not browser ..In browser it gives 6

console.log(3 
    + 3 )      // code readability should be high  as it runs buts don't write this type of code

    console.log("Prerna")

    let name = "Prerna"
    let age=21
    let isLoggedIn=false
    let state;

    // number => 2 to the power 53
    // bigint
    //string => ""
    //boolean => true/false
    //undefined =>
    //null=>standalone value
    


    //symbol =>unique

    console.log(typeof age)         //we can check what is the datatype by asking variable name
    console.log(typeof false)       //or we can also check what is the datatype by putting assingning data



    console.log(typeof null);   // null is a object

    console.log(typeof undefined);  //undefined

